namespace <%= schema_name %> {
    using System.Text;

    public class Root {
        <% [['Query', schema.query_root_name], ['Mutation', schema.mutation_root_name]].each do |operation_type, root_name| %>
        public static <%= root_name %>Query build<%= operation_type %>() {
            return new <%= root_name %>Query();
        }
        <% end %>
    }
}
