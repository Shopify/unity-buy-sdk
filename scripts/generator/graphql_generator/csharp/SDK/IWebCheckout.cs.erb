namespace <%= namespace %>.SDK {
    using System;
    
    interface IWebCheckout {
        void Checkout(string checkoutURL, CheckoutSuccessCallback success, CheckoutCancelCallback cancelled, CheckoutFailureCallback failure);
    }
}
