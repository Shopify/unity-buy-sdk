namespace <%= namespace %>.SDK {
    using System;
    
    interface IWebCheckout {
        void Checkout(string checkoutURL, CheckoutSuccessCallback success, CheckoutFailureCallback failure, CheckoutCancelCallback cancelled);
    }
}
