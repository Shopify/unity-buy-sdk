#if UNITY_ANDROID 
namespace <%= namespace %>.SDK.Android {
    using System;
    using System.Runtime.InteropServices;

    class AndroidPluginTest {
        public static void SayHello() {
            var ajc = new AndroidJavaClass("com.shopify.unity.buy.Say");
            var result = ajc.CallStatic("Say");
            Debug.Log("Java says: " + result);
        }
    }
}
#endif
