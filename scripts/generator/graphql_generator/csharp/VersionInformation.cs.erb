namespace Shopify.Unity {
  public class VersionInformation {
    /// <summary>
    /// Version of the Shopify SDK for Unity.
    /// </summary>
    /// \code
    /// // Example that checks the SDK version
    /// Debug.Log("Current SDK Version: " + ShopifyBuy.VERSION);
    /// \endcode
    public const string VERSION = "<%= version %>-<%= publish_destination %>";
    public const string PUBLISH_DESTINATION = "<%= publish_destination %>";
  }
}
